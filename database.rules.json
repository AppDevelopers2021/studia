{
  "rules": {
    ".read": false,
    ".write": false,

    "calendar": {
      "$uid": {
        ".read": "auth != null && auth.uid == $uid",
        ".write": "auth != null && auth.uid == $uid"
      }
    },
    "notifications": {
      ".read": true,
      ".write": false
    }
  }
}