{
  "rules": {
    ".read": false,
    ".write": true,

    "calendar": {
      "$uid": {
        ".read": "auth != null && auth.uid == $uid",
        ".write": "auth != null && auth.uid == $uid"
      }
    },
    "notifications": {
      ".read": true,
      ".write": false
    }
  }
}